<%= form_for @event, :url => addguest_path do |f| %>
    <%= f.hidden_field :event_id, { :value => @event.id } %>
    <%= f.hidden_field :user_id, {:value => current_user.id} %>
    <%= f.hidden_field :organizer, { :value => false } %>
    <%= f.submit "Sign Up!" %>
<%end%>